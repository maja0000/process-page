import React from 'react';
import './TechStack.css';
import '../../App.css';

export default function TechStack() {
  return (
    <>
      <div className="stack">
        <p
          className="bounce"
          style={{ color: '#ED8383', fontSize: '5vw', marginLeft: '10%' }}
        >
          mockups
        </p>
        <p style={{ width: '70%', marginLeft: '60px' }}>
          ttPlain text, Plain-text, or Plaintext is any text, text file, or
          document that contains only text. Unlike a rich-text document, a plain
          text file cannot have bold text, fonts, larger font sizes, or any
          other special text fhat cocument that contains only text. Unlike a
          rich-text document, a plain text file cannot have bold text, fonts,
          larger font sizes, or any other special text fhat contains only text.
          Unlike a rich-text document, a plain text file cannot have bold text,
          fonts, larger font sizes, or any other special ntains only text.
          Unlike a rich-text document, a plain text file cannot have bold text,
          fonts, larger font sizes, or any other special text formatting. In the
          picture is a visual example of plain text vs.Oct 17, 2017
        </p>
      </div>
    </>
  );
}
